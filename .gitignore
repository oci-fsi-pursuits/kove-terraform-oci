# Terraform
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
# Local secrets (terraform.tfvars.example is committed as a template only)
terraform.tfvars
terraform.tfvars.json
*.local.tfvars
secrets.auto.tfvars
# Var-files saved as .txt (e.g. Downloads copies) — ignore if placed in repo
*.tfvars.txt
# Packed playbooks (generated when run_ansible_from_head = true)
playbooks.zip
.terraform/playbooks.zip
# OCI Resource Manager job exports (browser download names often ocid1.ormjob....zip)
ocid1.ormjob*.zip
